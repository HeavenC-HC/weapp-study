import { View } from '@tarojs/components';
import React from 'react';

function Index(props) {
    return (
        <View>
            {props.dataList.a}
        </View>
    );
}

export default Index;